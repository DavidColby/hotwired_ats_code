<div>
  <div class="flex flex-col md:flex-row items-center p-4 justify-between">
    <div class="md:w-1/2">
      <%= link_to job.title, careers_job_path(job), class: "text-lg text-blue-600 hover:text-blue-700" %>
    </div>
    <div class="md:w-1/3">
      <%= job.job_type.humanize %> in <%= job.location %>
    </div>
    <div class="text-right">
      <%= link_to "View Details", careers_job_path(job), class: "btn border border-blue-200 hover:bg-blue-100 text-sm text-blue-700" %>
    </div>
  </div>
</div>
